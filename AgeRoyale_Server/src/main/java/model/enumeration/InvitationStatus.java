package model.enumeration;

/** Representa la enum InvitationStatus, esta contiene el estado de una invitación, ya sea de amistad o de partida */
public enum InvitationStatus {
    Pending,
    Refuse,
    Accepted
}